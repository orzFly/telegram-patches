diff --git a/Telegram/SourceFiles/api/api_blocked_peers.cpp b/Telegram/SourceFiles/api/api_blocked_peers.cpp
index 1e7915e..8cab516 100644
--- a/Telegram/SourceFiles/api/api_blocked_peers.cpp
+++ b/Telegram/SourceFiles/api/api_blocked_peers.cpp
@@ -191,6 +191,7 @@ void BlockedPeers::reload() {
 		return;
 	}
 	request(0, [=](Slice &&slice) {
+		applyBlockedStatus(slice);
 		if (!_slice || *_slice != slice) {
 			_slice = slice;
 			_changes.fire(std::move(slice));
@@ -198,6 +199,20 @@ void BlockedPeers::reload() {
 	});
 }
 
+void BlockedPeers::applyBlockedStatus(const Slice &slice, int offset) {
+	for (const auto &item : slice.list) {
+		if (const auto peer = _session->data().peerLoaded(item.id)) {
+			peer->setIsBlocked(true);
+		}
+	}
+	const auto loaded = offset + int(slice.list.size());
+	if (loaded < slice.total) {
+		request(loaded, [=](Slice &&next) {
+			applyBlockedStatus(next, loaded);
+		});
+	}
+}
+
 auto BlockedPeers::slice() -> rpl::producer<BlockedPeers::Slice> {
 	if (!_slice) {
 		reload();
diff --git a/Telegram/SourceFiles/api/api_blocked_peers.h b/Telegram/SourceFiles/api/api_blocked_peers.h
index ca2000f..9a9a9c6 100644
--- a/Telegram/SourceFiles/api/api_blocked_peers.h
+++ b/Telegram/SourceFiles/api/api_blocked_peers.h
@@ -59,6 +59,8 @@ private:
 		bool blocking,
 		Fn<void(bool success)> done = nullptr);
 
+	void applyBlockedStatus(const Slice &slice, int offset = 0);
+
 	const not_null<Main::Session*> _session;
 
 	MTP::Sender _api;
diff --git a/Telegram/SourceFiles/apiwrap.cpp b/Telegram/SourceFiles/apiwrap.cpp
index ccc2891..446ff45 100644
--- a/Telegram/SourceFiles/apiwrap.cpp
+++ b/Telegram/SourceFiles/apiwrap.cpp
@@ -212,6 +212,8 @@ ApiWrap::ApiWrap(not_null<Main::Session*> session)
 
 		_reactionsNotifySettings->reload();
 		setupSupportMode();
+
+		_blockedPeers->reload();
 	});
 }
 
diff --git a/Telegram/SourceFiles/data/data_send_action.cpp b/Telegram/SourceFiles/data/data_send_action.cpp
index ea8f92b..50c3027 100644
--- a/Telegram/SourceFiles/data/data_send_action.cpp
+++ b/Telegram/SourceFiles/data/data_send_action.cpp
@@ -53,6 +53,9 @@ void SendActionManager::registerFor(
 	if (history->peer->isSelf()) {
 		return;
 	}
+	if (user->isBlocked()) {
+		return;
+	}
 	const auto sendAction = lookupPainter(history, rootId);
 	if (!sendAction) {
 		return;
diff --git a/Telegram/SourceFiles/history/history.cpp b/Telegram/SourceFiles/history/history.cpp
index 7d5e250..7884051 100644
--- a/Telegram/SourceFiles/history/history.cpp
+++ b/Telegram/SourceFiles/history/history.cpp
@@ -1542,7 +1542,11 @@ void History::newItemAdded(not_null<HistoryItem*> item) {
 			peer->updateFullForced();
 		}
 	} else {
-		if (item->unread(this)) {
+		const auto fromBlocked = !item->out()
+			&& !item->isService()
+			&& (peer->isChat() || peer->isMegagroup())
+			&& item->from()->isBlocked();
+		if (item->unread(this) && !fromBlocked) {
 			if (unreadCountKnown()) {
 				setUnreadCount(unreadCount() + 1);
 			} else if (!isForum()) {
@@ -2884,10 +2888,35 @@ auto History::computeChatListMessageFromLast() const
 		return _lastMessage;
 	}
 
+	const auto last = lastMessage();
+	const auto isFromBlockedSender = [&](not_null<HistoryItem*> item) {
+		return !item->out()
+			&& !item->isService()
+			&& (peer->isChat() || peer->isMegagroup())
+			&& item->from()->isBlocked();
+	};
+
+	if (last && isFromBlockedSender(last)) {
+		if (!loadedAtBottom()) {
+			return std::nullopt;
+		}
+		for (const auto &block : ranges::views::reverse(blocks)) {
+			for (const auto &item : ranges::views::reverse(block->messages)) {
+				const auto msg = item->data();
+				if (!isFromBlockedSender(msg) && !msg->isGroupMigrate()) {
+					return msg;
+				}
+			}
+		}
+		if (loadedAtTop()) {
+			return _lastMessage;
+		}
+		return std::nullopt;
+	}
+
 	// In migrated groups we want to skip essential message
 	// about migration in the chats list and display the last
 	// non-migration message from the original legacy group.
-	const auto last = lastMessage();
 	if (!last || !last->isGroupMigrate()) {
 		return _lastMessage;
 	}
diff --git a/Telegram/SourceFiles/history/history_item.cpp b/Telegram/SourceFiles/history/history_item.cpp
index 761a861..68e346f 100644
--- a/Telegram/SourceFiles/history/history_item.cpp
+++ b/Telegram/SourceFiles/history/history_item.cpp
@@ -4611,7 +4611,20 @@ void HistoryItem::updateReactions(const MTPMessageReactions *reactions) {
 		return;
 	}
 	const auto hasUnread = _reactions && _reactions->hasUnread();
-	if (hasUnread && !hadUnread) {
+	const auto hasNonBlockedUnread = hasUnread && [&] {
+		if (_history->peer->isUser()) {
+			return true;
+		}
+		for (const auto &[id, reactions] : recentReactions()) {
+			for (const auto &r : reactions) {
+				if (r.unread && !r.peer->isBlocked()) {
+					return true;
+				}
+			}
+		}
+		return false;
+	}();
+	if (hasNonBlockedUnread && !hadUnread) {
 		_flags |= MessageFlag::HasUnreadReaction;
 
 		addToUnreadThings(HistoryUnreadThings::AddType::New);
diff --git a/Telegram/SourceFiles/history/history_item_components.cpp b/Telegram/SourceFiles/history/history_item_components.cpp
index d671523..8400bc6 100644
--- a/Telegram/SourceFiles/history/history_item_components.cpp
+++ b/Telegram/SourceFiles/history/history_item_components.cpp
@@ -501,6 +501,10 @@ void HistoryMessageReply::updateData(
 				// Really it is deleted.
 				resolvedMessage = nullptr;
 				force = true;
+			} else if (!resolvedMessage->out()
+					&& resolvedMessage->from()->isBlocked()) {
+				resolvedMessage = nullptr;
+				force = true;
 			} else {
 				holder->history()->owner().registerDependentMessage(
 					holder,
diff --git a/Telegram/SourceFiles/history/view/history_view_element.cpp b/Telegram/SourceFiles/history/view/history_view_element.cpp
index e568895..3765430 100644
--- a/Telegram/SourceFiles/history/view/history_view_element.cpp
+++ b/Telegram/SourceFiles/history/view/history_view_element.cpp
@@ -1422,7 +1422,19 @@ bool Element::isHiddenByGroup() const {
 }
 
 bool Element::isHidden() const {
-	return isHiddenByGroup();
+	if (isHiddenByGroup()) {
+		return true;
+	}
+	const auto item = data();
+	const auto peer = history()->peer;
+	if (!item->isService()
+		&& !item->out()
+		&& (peer->isChat() || peer->isMegagroup())) {
+		if (item->from()->isBlocked()) {
+			return true;
+		}
+	}
+	return false;
 }
 
 void Element::overrideMedia(std::unique_ptr<Media> media) {
@@ -2172,6 +2184,9 @@ QSize Element::countCurrentSize(int newWidth) {
 	if (_flags & Flag::NeedsResize) {
 		initDimensions();
 	}
+	if (isHidden()) {
+		return { newWidth, 0 };
+	}
 	return performCountCurrentSize(newWidth);
 }
 
diff --git a/Telegram/SourceFiles/history/view/reactions/history_view_reactions.cpp b/Telegram/SourceFiles/history/view/reactions/history_view_reactions.cpp
index 130b8eb..5f7a902 100644
--- a/Telegram/SourceFiles/history/view/reactions/history_view_reactions.cpp
+++ b/Telegram/SourceFiles/history/view/reactions/history_view_reactions.cpp
@@ -916,6 +916,25 @@ InlineListData InlineListDataFromMessage(not_null<Element*> view) {
 	auto result = InlineListData();
 	result.reactions = item->reactionsWithLocal();
 
+	{
+		const auto &recent = item->recentReactions();
+		for (auto &reaction : result.reactions) {
+			const auto i = recent.find(reaction.id);
+			if (i != recent.end()) {
+				for (const auto &r : i->second) {
+					if (r.peer->isBlocked()) {
+						--reaction.count;
+					}
+				}
+			}
+		}
+		result.reactions.erase(
+			ranges::remove_if(result.reactions, [](const auto &r) {
+				return r.count <= 0 && !r.my;
+			}),
+			result.reactions.end());
+	}
+
 	const auto shouldAddEmptyPaidButton = [&] {
 		if (view->context() == Context::ChatPreview) {
 			return false;
@@ -958,8 +977,19 @@ InlineListData InlineListDataFromMessage(not_null<Element*> view) {
 		}
 	} else {
 		const auto &recent = item->recentReactions();
+		auto filtered = base::flat_map<
+			ReactionId,
+			std::vector<not_null<PeerData*>>>();
+		for (const auto &[id, list] : recent) {
+			auto &peers = filtered[id];
+			for (const auto &reaction : list) {
+				if (!reaction.peer->isBlocked()) {
+					peers.push_back(reaction.peer);
+				}
+			}
+		}
 		const auto showUserpics = [&] {
-			if (recent.size() != result.reactions.size()) {
+			if (filtered.size() != result.reactions.size()) {
 				return false;
 			}
 			auto sum = 0;
@@ -967,20 +997,16 @@ InlineListData InlineListDataFromMessage(not_null<Element*> view) {
 				if ((sum += reaction.count) > kMaxRecentUserpics) {
 					return false;
 				}
-				const auto i = recent.find(reaction.id);
-				if (i == end(recent) || reaction.count != i->second.size()) {
+				const auto i = filtered.find(reaction.id);
+				if (i == end(filtered)
+					|| reaction.count != int(i->second.size())) {
 					return false;
 				}
 			}
 			return true;
 		}();
 		if (showUserpics) {
-			result.recent.reserve(recent.size());
-			for (const auto &[id, list] : recent) {
-				result.recent.emplace(id).first->second = list
-					| ranges::views::transform(&Data::RecentReaction::peer)
-					| ranges::to_vector;
-			}
+			result.recent = std::move(filtered);
 		}
 	}
 	result.flags = (view->hasOutLayout() ? Flag::OutLayout : Flag())
diff --git a/Telegram/SourceFiles/window/notifications_manager.cpp b/Telegram/SourceFiles/window/notifications_manager.cpp
index 6ab4949..e3ee9aa 100644
--- a/Telegram/SourceFiles/window/notifications_manager.cpp
+++ b/Telegram/SourceFiles/window/notifications_manager.cpp
@@ -347,17 +347,21 @@ System::SkipState System::computeSkipState(
 	if (messageType && notifySettings->muteUnknown(thread)) {
 		return { SkipState::Unknown };
 	} else if (messageType && !notifySettings->isMuted(thread)) {
+		if (!item->out()
+			&& item->from()->isBlocked()
+			&& !thread->peer()->isUser()) {
+			return { SkipState::Skip };
+		}
 		return withSilent(SkipState::DontSkip);
 	} else if (!notifyBy) {
 		return withSilent(
 			showForMuted ? SkipState::DontSkip : SkipState::Skip,
 			showForMuted);
 	} else if (notifySettings->muteUnknown(notifyBy)
-		|| (!messageType
-			&& notifyBy->blockStatus() == PeerData::BlockStatus::Unknown)) {
+		|| notifyBy->blockStatus() == PeerData::BlockStatus::Unknown) {
 		return withSilent(SkipState::Unknown);
 	} else if (!notifySettings->isMuted(notifyBy)
-		&& (messageType || !notifyBy->isBlocked())) {
+		&& !notifyBy->isBlocked()) {
 		return withSilent(SkipState::DontSkip);
 	} else {
 		return withSilent(
